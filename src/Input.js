export default class Input {
  constructor() {
    //to do
    // this.area = area;
  }
  //getAxisRaw
  //Horizontal = {left:"a",right:"d"}
  //Vertical = {up:"w",down:"s"}
  static area(side) {
    return side * side;
  }
}

// let area = (side) => side * side;

// export { area };
